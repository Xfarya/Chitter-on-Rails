<p> <%= @peep.body %> </p>

<ul>
  <li><%= link_to "Edit", edit_peep_path(@peep) %></li>
  <li><%= link_to "Delete", peep_path(@peep), method: :delete, data: { confirm: "Are you sure?" }%></li>
</ul>

<%# <h2>Comments</h2> %>
<%= render @peep.comments %>

<h3>respond</h3>
<%= render 'comments/form' %>